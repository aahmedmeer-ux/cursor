# Optional paid AI proxy (not used by default)

The Jettribe theme Virtual Try-On is **free by default**: it runs entirely in the
shopper's browser with MediaPipe pose detection + canvas compositing.

This Cloudflare Worker folder is only if you later want a paid generative AI
provider (e.g. fal.ai). It is **not required** for Try It to work.
